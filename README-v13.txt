Gym Tracker v13.0

Install the files into the existing repo and rebuild the curated library from your local exercise dataset:

python3 prepare-exercise-library.py --source ~/gym-exercise-lookup --dest ~/gym-tracker

This release builds a 250-exercise pool. It does not delete existing images/videos, so older saved workouts remain supported.
