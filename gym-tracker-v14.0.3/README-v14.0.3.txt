Gym Tracker v14.0.3

Fixes:
- Fixes the remaining interactive body-map Set lookup bug.
- Removes stale service-worker/cache interference from previous app versions.
- Adds a visible JS error state instead of leaving the app on "Loading".
- Keeps existing localStorage data and exercise library intact.

Do not run prepare-exercise-library.py unless you are intentionally rebuilding the library.
